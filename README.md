# 3NMOS PWM 控制器（ESP32-S3 离线固件）

基于 ESP32-S3 的 3 路独立 PWM 控制器，**完全离线运行**（不联网、无 MQTT）。手机连接设备热点即可在本地配置页面设置参数，由一路数字输入信号的高低电平驱动 3 路 PWM 输出，电平切换时占空比硬件平滑过渡。

## 功能特性

- **离线运行**：上电即按本地配置工作，无需路由器或服务器。
- **本地配置页**：SoftAP + Captive Portal，手机连热点自动弹出配置页面。
- **3 路独立 PWM**：每路独立设置高/低电平状态的频率与占空比。
- **平滑切换**：输入电平变化时占空比硬件 fade 平滑过渡、频率瞬间切换；高→低、低→高 各自独立切换时间。
- **双反向开关**：输入电平逻辑取反、PWM 输出极性取反（独立可配）。
- **按键控制**：短按 LED 反馈，长按 5 秒进入配置页面。
- **掉电保存**：所有参数存于 NVS，断电不丢失。

## 硬件

- 芯片：ESP32-S3
- 日志输出：UART0（TX=GPIO43 / RX=GPIO44）

### 引脚定义

| 引脚 | 功能 | 说明 |
|------|------|------|
| GPIO0  | 按键       | 内部上拉，按下拉低（strapping 脚，**上电时勿按住**）|
| GPIO15 | LED        | 低电平点亮 |
| GPIO10 | 输入信号   | 与 GPIO9 二选一（配置项，默认）|
| GPIO9  | 输入信号   | 与 GPIO10 二选一（配置项）|
| GPIO48 | PWM1       | LEDC，独立频率/占空比 |
| GPIO21 | PWM2       | LEDC，独立频率/占空比 |
| GPIO47 | PWM3       | LEDC，独立频率/占空比 |

> 所有引脚均为 ESP32-S3 自由普通 GPIO，已避开 strapping / SPI-flash / USB / UART 冲突。

### LED 状态

| 状态 | LED 表现 |
|------|----------|
| 工作模式     | 熄灭 |
| 配置模式     | 慢闪 1Hz |
| 短按按键     | 快闪 1 次 |
| 长按计时中   | 持续快闪 |
| 长按满 5 秒  | 常亮 1 秒后重启进配置 |

## 使用方法

### 1. 配置参数（首次必做）

1. 上电。首次（NVS 为空）会自动进入配置模式；之后可**长按按键 5 秒**进入。
2. 手机连接热点 `3NMOS-XXXXXX`（XXXXXX 为设备 MAC 后缀，无密码）。
3. 浏览器自动弹出配置页，或手动访问 `http://192.168.4.1`。
4. 配置参数（见下表），点击「保存并重启」。

| 配置项 | 范围 | 默认 |
|--------|------|------|
| 输入信号引脚        | GPIO10 / GPIO9            | GPIO10 |
| 输入电平反向        | 开 / 关                   | 关 |
| PWM 极性反向（3 路共享）| 开 / 关                 | 关 |
| 各路 PWM 低电平频率  | 1 ~ 40000 Hz             | 1000 Hz |
| 各路 PWM 低电平占空比| 0.0 ~ 100.0 %            | 50.0% |
| 各路 PWM 高电平频率  | 1 ~ 40000 Hz             | 1000 Hz |
| 各路 PWM 高电平占空比| 0.0 ~ 100.0 %            | 50.0% |
| 低→高 切换时间       | 0 ~ 65535 ms             | 300 ms |
| 高→低 切换时间       | 0 ~ 65535 ms             | 300 ms |

### 2. 工作模式

保存重启后进入工作模式。设备根据输入信号电平输出对应状态的 3 路 PWM：

- 输入**低电平** → 输出"低电平态"频率/占空比
- 输入**高电平** → 输出"高电平态"频率/占空比
- 电平切换时，占空比按配置时间平滑过渡（硬件 fade），频率瞬间切换

### 3. 恢复默认参数

配置页面点击「恢复默认参数」可重置全部参数为出厂值并重启（重启后仍停留在配置页）。

## 编译

### GitHub Actions（推荐）

推送代码到 `main` 分支会自动触发编译（`.github/workflows/build.yml`），使用 ESP-IDF v5.2 容器，产物为 `merged.bin`，在 Actions 运行页下载 artifact。

### 本地编译

```bash
. $IDF_PATH/export.sh
idf.py set-target esp32s3
idf.py build
```

## 烧录

`merged.bin` 为合并固件（bootloader + 分区表 + 应用），单地址烧录：

```bash
esptool.py --chip esp32s3 --port COM3 --baud 460800 write_flash 0x0 merged.bin
```

（COM3 替换为实际串口号）

## 项目结构

```
main/
├── app.c / app.h          # 主程序入口、模式判定、引脚配置
├── nvs_param.c / .h       # NVS 参数读写（配置持久化）
├── pwm_ctrl.c / .h        # 3 路 LEDC + 动态分辨率 + 硬件 fade
├── input_sig.c / .h       # 输入信号轮询 + 消抖 + 反向
├── config_portal.c / .h   # SoftAP 配置页面（httpd + Captive Portal + DNS 劫持）
└── key.c / .h             # 按键状态机（短按反馈 / 长按进配置）
CMakeLists.txt             # 工程根（project(3nmos_no_wifi)）
sdkconfig.defaults         # ESP32-S3 默认配置
.github/workflows/build.yml
```

## 技术说明

- **LEDC 动态分辨率**：按目标频率自动选择 `duty_resolution`，使分频系数落在 `[2, 1024]`，覆盖 1 Hz ~ 40 kHz 全段并尽量提高占空比精度。
- **平滑切换**：用 LEDC 硬件 fade（`ledc_set_fade_with_time`）平滑占空比；频率变化时重配定时器实现瞬切，并对当前占空比 tick 做分辨率缩放消除输出跳变。
- **占空比存储**：百分比 0.0~100.0 在 NVS 以 `uint16_t ×10` 存储（如 33.3% → 333）。

## License

MIT License
